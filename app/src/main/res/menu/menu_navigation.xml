<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/favorites"
        android:icon="@drawable/favorites_icon"
        android:title="@string/menu_favorites_title"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/watch_later"
        android:icon="@drawable/watch_later_icon"
        android:title="@string/menu_watch_later_title"
        app:showAsAction="ifRoom"/>
    <item
        android:id="@+id/podborki"
        android:icon="@drawable/podborki_icon"
        android:title="@string/menu_podborki_title"
        app:showAsAction="ifRoom"/>
</menu>